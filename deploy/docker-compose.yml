version: '3.8'

services:
  db:
    container_name: ${PROJECT_NAME}_database
    image: postgres:14
    volumes:
      - ${PROJECT_NAME}_database_data:/var/lib/postgresql/data
    ports:
      - ${POSTGRES_HOST_PORT}:${POSTGRES_PORT}
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    restart: on-failure

  rabbitmq-image:
    container_name: ${PROJECT_NAME}_rabbitmq_image
    image: rabbitmq:3.11.10-management
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBIT_USERNAME}
      RABBITMQ_DEFAULT_PASS: ${RABBIT_PASSWORD}
    ports:
      - ${RABBIT_HOST_PORT}:${RABBIT_PORT}
      - ${RABBIT_MANAGE_HOST_PORT}:${RABBIT_MANAGE_PORT}
    volumes:
      - ${PROJECT_NAME}_rabbitmq_data:/var/lib/rabbitmq
    restart: always
    healthcheck:
        test: ["CMD", "rabbitmqctl", "status"]
        interval: 30s
        timeout: 10s
        retries: 5

  tg_bot:
    container_name: ${PROJECT_NAME}_tg_bot
    image: ${BASE_IMAGE}
    build:
      context: ../
      dockerfile: deploy/Dockerfile
    command: ["make", "migrate-up", "&&", "make", "run-tg-bot"]
    env_file:
      - .env
    depends_on:
      - db
      - rabbitmq
    restart: on-failure
    
  
  admin_api:
    container_name: ${PROJECT_NAME}_admin_api
    image: ${BASE_IMAGE}
    build:
      context: ../
      dockerfile: deploy/Dockerfile
    command: ["make", "migrate-up", "&&", "make", "run-admin-api"]
    env_file:
      - .env
    ports:
      - ${ADMIN_API_HOST_PORT}:${ADMIN_API_PORT}
    depends_on:
      - db
      - tg_bot
    restart: on-failure

  rabbitmq:
    container_name: ${PROJECT_NAME}_rabbitmq
    image: ${BASE_IMAGE}
    build:
      context: ../
      dockerfile: deploy/Dockerfile
    command: ["make", "migrate-up", "&&", "make", "run-rabbitmq"]
    env_file:
      - .env
    depends_on:
      rabbitmq-image:
        condition: service_healthy

    restart: on-failure
  
volumes:
 black_jack_database_data:
 black_jack_rabbitmq_data: